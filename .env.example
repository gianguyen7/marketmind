# Polymarket API (CLOB)
POLYMARKET_API_URL=https://clob.polymarket.com
# Optional: API key if using authenticated endpoints
POLYMARKET_API_KEY=

# Data paths (defaults work without setting these)
DATA_DIR=data
OUTPUT_DIR=outputs
